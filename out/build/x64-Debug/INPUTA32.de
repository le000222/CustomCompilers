main() {
	//testing variable declarations. Can have assignment statements. 
	int i = 0, age = 24, Test_Variable_01;
	float a, sum008 = 50.66, Test_Floating_Point;
	string text = 'text', name = 'Eri', partner = 'Dan';

	//testing assignment statements
	a=+1.2;
	sum008 = 7.870 ;

	//test for input method multi argument
	input(a,sum008);
	i=0;
	//testing compound statements
	do{
	   i = i + 2;
	   a = a * i/0.5;
	   sum008 = sum008 + a - 1 ;
	}while (i < 32767 | i == 32767);

	if (text == '') {
	    text = 'prog' + 'ram';
	}
	else {
	    text = text + 'ram';
	}

	//testing compount statements and syntax
	while(true){
	   do{
	      input(Test_Variable_01);
	      Test_Variable_01 = a/sum088;
	      while(sum088 * a <> Test_Floating_Point) {
		 print('this should be a syntax error but this program is tough on tokens');
		 int is_A_SyntaxError;
		 if (sum088 * a <> Test_Floating_Point){
		 is_A_SyntaxError = true;
		 } else {
		   print('this program doesn"t actually work but it likes to eat tokens');
                 }
		 int having_Fun;
              }
	   }while(having_Fun);		
	}

<! Testing block comments Testing block comments Testing block comments Testing block comments Testing block comments
Testing block comments Testing block comments Testing block comments Testing block comments Testing block comments 
Testing block comments Testing block comments Testing block comments
!>
	print('\* This is a program for fun with no execution -:)-<-<-- \*');
	print(text);
	if (text <> 'program' & sum008 == 8.0 | i>10) {
	    print(sum008);
	    print();
	}
	else{
	  ;
	}

}